-- ============================================
-- Migration 093: 新增 Trello 同步排程設定
-- 目標：支援用戶自訂 Trello 同步時段
-- 執行日期：2025-12-04
-- ============================================

-- ========================================
-- 1. 在 system_settings 表中儲存 Trello 同步時段
-- ========================================

-- 預設每兩小時同步：00:00, 02:00, 04:00, 06:00, 08:00, 10:00, 12:00, 14:00, 16:00, 18:00, 20:00, 22:00
INSERT INTO system_settings (key, value, updated_at)
VALUES (
  'trello_sync_schedule',
  '["08:00", "10:00", "12:00", "14:00", "16:00", "18:00", "20:00", "22:00"]',
  NOW()
)
ON CONFLICT (key) DO UPDATE SET
  value = EXCLUDED.value,
  updated_at = NOW();

-- ========================================
-- 完成
-- ========================================

SELECT '✅ Migration 093 完成：已新增 Trello 同步排程設定' as status;
